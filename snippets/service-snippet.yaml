name: service
label: Create Kubernetes Service
description: Create Kubernetes Service
body: |2
  kind: Service
  apiVersion: v1
  metadata:
    name: ${1:<my-service1>}
  spec:
    selector:
      app: ${2:<MyApp1>}
    ports:
    - port: ${3:<Port>}
      targetPort: ${4:<Target Port>}